@model List<MenuCategory>

<h1>Kategoriler</h1>

<ul>
    @foreach (var category in Model)
    {
        <li><a href="@Url.Action("MenuList", new { categoryId = category.Id })">@category.Name</a></li>
    }
</ul>